// Set ENABLE_AUTH to false if you want to turn off the login page completely.
// Set ENABLE_AUTH to true to require login before accessing the application.
export const ENABLE_AUTH = true;

// Default credentials for login (Username: admin, Password: 123)
export const DEFAULT_CREDENTIALS = {
  username: 'admin',
  password: '123'
};
